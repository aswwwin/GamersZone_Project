===== Game.php =====
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Game extends Model
{
    use HasFactory;
    
    protected $table = 'games';
    protected $primaryKey = 'game_id';
    
    protected $fillable = [
        'game_name',
        'description',
        'publisher',
        'genre',
        'release_date',
        'image_url',
        'is_popular',
        'is_active'
    ];
    
    // Define relationships
    public function inGameCurrencies()
    {
        return $this->hasMany(InGameCurrency::class, 'game_id', 'game_id');
    }
    
    public function coupons()
    {
        return $this->hasMany(Coupon::class, 'game_id', 'game_id');
    }
}

===== InGameCurrency.php =====
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class InGameCurrency extends Model
{
    use HasFactory;
    
    protected $table = 'in_game_currencies';
    protected $primaryKey = 'currency_id';
    
    protected $fillable = [
        'game_id',
        'currency_name',
        'description',
        'image_url',
        'price',
        'value_amount',
        'is_active'
    ];
    
    // Define relationships
    public function game()
    {
        return $this->belongsTo(Game::class, 'game_id', 'game_id');
    }
    
    public function orderItems()
    {
        return $this->morphMany(OrderItem::class, 'item');
    }
}

===== Coupon.php =====
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Coupon extends Model
{
    use HasFactory;
    
    protected $table = 'coupons';
    protected $primaryKey = 'coupon_id';
    
    protected $fillable = [
        'game_id',
        'coupon_code',
        'coupon_name',
        'description',
        'discount_percent',
        'fixed_amount',
        'expiry_date',
        'image_url',
        'price',
        'is_active'
    ];
    
    // Define relationships
    public function game()
    {
        return $this->belongsTo(Game::class, 'game_id', 'game_id');
    }
    
    public function orderItems()
    {
        return $this->morphMany(OrderItem::class, 'item');
    }
}

------order----

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Order extends Model
{
    use HasFactory;
    
    protected $table = 'orders';
    protected $primaryKey = 'order_id';
    
    protected $fillable = [
        'user_id',
        'order_date',
        'total_amount',
        'status',
        'payment_method'
    ];
    
    protected $casts = [
        'order_date' => 'datetime',
        'total_amount' => 'decimal:2'
    ];
    
    // Define relationships
    public function user()
    {
        return $this->belongsTo(User::class, 'user_id', 'user_id');
    }
    
    public function orderItems()
    {
        return $this->hasMany(OrderItem::class, 'order_id', 'order_id');
    }
    
    public function paymentTransactions()
    {
        return $this->hasMany(PaymentTransaction::class, 'order_id', 'order_id');
    }
}